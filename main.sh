# Welcome to Debian

# Step 1: Remove the # from the beginning of the BELOW line, so the code runs
# npx  --yes gritty --port 8000 --command "bash ./ReplRoot/boot.sh" --auto-restart

# Step 2: Delete the below line.
node server

